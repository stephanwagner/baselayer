<?php

defined('ABSPATH') || exit;

/**
 * Child theme bootstrap.
 *
 * Parent FromScratch loads core assets. Built child assets in assets/css/main.css
 * and assets/js/main.js are enqueued automatically by the parent when present.
 *
 * Add site-specific PHP below. Override config by adding files under config/
 * (e.g. config/theme-design.php). Override templates via templates/.
 */
