<?php

/**
 * Block settings configuration overrides.
 *
 * Inserter defaults for allowed, hidden and favorite blocks, plus hard-disallowed
 * blocks and embed/group variations. Used by Settings → Theme → Blocks.
 *
 * Parent defaults:
 *   fromscratch/config/block-settings.php
 *
 * Only specify the settings you want to override.
 */

return [];
