/**
 * Block options overrides.
 *
 * Editor sidebar controls for core and custom blocks (margins, padding, width, …).
 *
 * Parent defaults:
 *   fromscratch/config/block-options.js
 *
 * Editor sidebar controls are bundled from the parent theme.
 * Child overrides are not merged yet — this file is a placeholder.
 */
