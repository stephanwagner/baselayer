# Config overrides

Add PHP files with the same names as in the parent theme `config/` directory.
They are merged on top of the parent config (child values win).

Examples: `theme-design.php`, `theme.php`, `theme-content.php`
