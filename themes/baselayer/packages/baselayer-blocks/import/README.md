# Block import starters

The theme catalog lives at:

`themes/baselayer/blocks/blocks-import.json`

Import it from **Blocks → Import / Export → Import theme catalog**. Definitions match by `type` + `settings.slug`; importing twice updates in place (no duplicates).
